---
title: I/O多路复用的艰辛历程
date: 2026-02-15 22:54:11
tags:
  - C/C++
---

在`Linux`中一切皆文件。对所有东西的操作都可以通过`read()`和`write()`两个`API`来完成。而它们操作的对象都是文件描述符`fd`。

`Linux`对网络的操作也属于`I/O`操作，同样可以用上面两个`API`来完成，同样是针对文件描述符来操作，但是对于专门用于网络操作的文件描述符又被称作**套接字`socket`。**

传统阻塞式一对一服务端代码

~~~C
~~~

